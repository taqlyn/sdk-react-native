name: Publish to NPM

on:
  release:
    types: [published]
  push:
    tags:
      - 'v*'
  workflow_dispatch:

concurrency:
  group: publish-npm-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write # Required for NPM Trusted Publishing (OIDC tokenless) & Sigstore provenance

jobs:
  publish:
    name: Publish to NPM
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Bun.js
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Cache bun dependencies
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install dependencies
        run: bun install --frozen-lockfile || bun install

      - name: Run code generation
        run: bun run codegen

      - name: Build library
        run: bun run build

      - name: Publish to NPM (Tokenless Trusted Publishing)
        # npm 11.5.1+ automatically mints an ephemeral OIDC token via GitHub Actions id-token: write
        # No NPM_TOKEN or NODE_AUTH_TOKEN secret is required or used!
        run: npm publish --provenance --access public
