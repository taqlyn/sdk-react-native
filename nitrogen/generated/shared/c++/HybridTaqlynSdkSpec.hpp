///
/// HybridTaqlynSdkSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `DeferredLinkPayload` to properly resolve imports.
namespace margelo::nitro::taqlyn { struct DeferredLinkPayload; }

#include <string>
#include "DeferredLinkPayload.hpp"
#include <optional>
#include <NitroModules/Promise.hpp>
#include <functional>

namespace margelo::nitro::taqlyn {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `TaqlynSdk`
   * Inherit this class to create instances of `HybridTaqlynSdkSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridTaqlynSdk: public HybridTaqlynSdkSpec {
   * public:
   *   HybridTaqlynSdk(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridTaqlynSdkSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridTaqlynSdkSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridTaqlynSdkSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual void configure(const std::string& clientId, const std::string& publicKeyId, const std::string& apiBaseUrl, const std::string& linkProcessingMode, const std::string& env) = 0;
      virtual std::shared_ptr<Promise<std::optional<DeferredLinkPayload>>> resolveDeferred() = 0;
      virtual void addLinkListener(const std::function<void(const DeferredLinkPayload& /* link */)>& listener) = 0;
      virtual void removeLinkListener() = 0;
      virtual void consume(const std::string& linkId) = 0;
      virtual void setReadyForNavigation(bool ready) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "TaqlynSdk";
  };

} // namespace margelo::nitro::taqlyn
